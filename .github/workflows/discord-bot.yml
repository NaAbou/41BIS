name: Discord Bot Message Collector

on:
  schedule:
    # Esegue ogni 5 minuti (cron usa UTC)
    - cron: "0 */1 * * *"
  workflow_dispatch: # Permette di lanciarlo manualmente
permissions:
  contents: write
jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Clona il repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Installa Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 3️⃣ Installa le dipendenze
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install discord.py

      # 4️⃣ Esegui lo script bot
      - name: Run Discord bot
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
        run: python bot.py

      # 5️⃣ (Opzionale) Commit dei messaggi nel repo
      - name: Commit & Push messages.json
        if: success()
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add messages.json
          git commit -m "🕒 Update messages.json [$(date -u +'%Y-%m-%d %H:%M:%S UTC')]" || echo "No changes to commit"
          git push
          